const state = {
    vouchers: []
}

const getters = {
}

const actions = {
    // addToCart({ commit }, vouchers) {
    //     commit('addVouchers', vouchers)
    // }
}

const mutations = {
    // addVouchers(state, vouchers) {
    //     state.push(vouchers)
    // }
}

export default {
    namespaced: true,
	state,
	mutations,
	actions,
	getters
}